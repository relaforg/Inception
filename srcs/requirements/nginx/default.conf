server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name relaforg.42.fr;

    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    root /var/www/html;
    index index.php;

    location / {
	try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
	fastcgi_pass wordpress:9000;
	fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
	include fastcgi_params;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name adminer.relaforg.42.fr;

    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
	proxy_pass http://adminer:8081;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name gitea.relaforg.42.fr;

    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
	proxy_pass http://gitea:3000;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name static.relaforg.42.fr;

    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
	proxy_pass http://static_site:80;
    }
}
